# Использование официального образа Python 3.10 как родительского образа
FROM python:3.10-slim

# Установка рабочего каталога в контейнере (в данном случае, это директория проекта)
WORKDIR .

# Копирование файлов проекта и виртуального окружения в контейнер
COPY . .

# Активация виртуального окружения
RUN pip install --no-cache-dir -r requirements.txt


# Запуск приложения при старте контейнера
CMD ["python3", "./subproc.py"]
